interface:
  display_name: "Sync Codex History"
  short_description: "Keep local Codex history visible after account switches."
  default_prompt: "Use $sync-codex-history to restore local Codex history visibility after switching accounts or API login."
